[pytest]
testpaths = tests
markers =
    integration: real KPipeline/torch/espeak-ng synthesis tests (slow, skipped by default)
    slow: opt-in tests that need minutes or gigabytes (the >4 GB bundle round trip); run with -m slow
addopts = -m "not integration and not slow" --strict-markers
