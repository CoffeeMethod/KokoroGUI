"""Audio-side helpers for the Qt shell: the position-tracking `Transport`
(transport.py) that plays a whole arrangement, as opposed to `playback.py`'s
fire-and-forget preview/JIT player, and the shared block mixer both it and
the offline exporter (`kokoro_gui.daw.mixdown`) use.
"""
